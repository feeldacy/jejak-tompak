# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: accessibility.spec.js >> Accessibility >> all images should have alt attributes
- Location: tests\accessibility.spec.js:14:3

# Error details

```
Test timeout of 30000ms exceeded while running "beforeEach" hook.
```

# Test source

```ts
  1  | // @ts-check
  2  | import { test, expect } from "@playwright/test";
  3  | 
  4  | test.describe("Accessibility", () => {
> 5  |   test.beforeEach(async ({ page }) => {
     |        ^ Test timeout of 30000ms exceeded while running "beforeEach" hook.
  6  |     await page.goto("/", { waitUntil: "networkidle" });
  7  |   });
  8  | 
  9  |   test("should have proper document language attribute", async ({ page }) => {
  10 |     const lang = await page.locator("html").getAttribute("lang");
  11 |     expect(lang).toMatch(/id|en/);
  12 |   });
  13 | 
  14 |   test("all images should have alt attributes", async ({ page }) => {
  15 |     // Wait for content to load
  16 |     await page.waitForLoadState("domcontentloaded");
  17 |     const images = page.locator("img");
  18 |     const count = await images.count();
  19 | 
  20 |     for (let i = 0; i < count; i++) {
  21 |       const alt = await images.nth(i).getAttribute("alt");
  22 |       expect(
  23 |         alt,
  24 |         `Image at index ${i} is missing alt attribute`,
  25 |       ).not.toBeNull();
  26 |     }
  27 |   });
  28 | 
  29 |   test("should have no broken links on the homepage", async ({ page }) => {
  30 |     await page.waitForLoadState("domcontentloaded");
  31 |     const links = page.locator("a[href]");
  32 |     const count = await links.count();
  33 | 
  34 |     for (let i = 0; i < Math.min(count, 10); i++) {
  35 |       const href = await links.nth(i).getAttribute("href");
  36 |       expect(href, `Link at index ${i} has empty href`).not.toBe("");
  37 |     }
  38 |   });
  39 | 
  40 |   test("page should be scrollable without horizontal overflow", async ({
  41 |     page,
  42 |   }) => {
  43 |     const body = page.locator("body");
  44 |     const scrollWidth = await body.evaluate((el) => el.scrollWidth);
  45 |     const clientWidth = await body.evaluate((el) => el.clientWidth);
  46 |     expect(scrollWidth).toBeLessThanOrEqual(clientWidth + 1);
  47 |   });
  48 | });
  49 | 
```